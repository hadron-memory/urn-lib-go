# fixtures

`corpus.json` is a SYNCED COPY of the canonical corpus in
[`urn-lib-js/fixtures/corpus.json`](https://github.com/hadron-memory/urn-lib-js/blob/main/fixtures/corpus.json).
Update both in the same change until a shared `urn-lib-fixtures` package exists.
